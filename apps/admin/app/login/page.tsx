export const metadata = {
  title: 'Login',
};

export default function LoginPage() {
  return (
    <main>
      <section className="shell">
        <h1>Admin login</h1>
        <p>Authentication will be connected after the Platform and IAM foundation is complete.</p>
      </section>
    </main>
  );
}
